# 작업 로그 — 2026-07-31

가맹거래사 1차 시험대비 앱 "스터디박스" (gamyeong-exam) — 다른 PC(집)에서 진행된 작업을 받아온 뒤, 모바일/PC 레이아웃 불일치를 통일한 세션. 전날(7/30) 작업 전체 맥락은 `WORKLOG_2026-07-30.md` 참고.

## 배경

7/30 낮~저녁 세션에서 홈 화면을 OTT 스타일로 개편하고 인트로 영상(`assets/videos/studybox_intro.mp4`)까지 넣은 뒤, **다른 PC(집)에서 이어서 작업**이 진행되어 있었음:
- 기존 `widgets/encourage_bar.dart` + `widgets/intro_video.dart` + `widgets/vertical_rolling_banner.dart`를 통합해 **`widgets/video_strip_banner.dart`(VideoStripBanner)** 신규 — 쿠팡플레이 스타일로 영상이 화면 맨 위에서 시작하고, 그 위에 타이틀/CTA/메뉴가 오버레이되는 히어로 배너.
- `widgets/nav_items.dart` 신규 — 네비게이션 항목(`NavItem`, `kNavItems`) 공통 정의 분리.
- PC(`_buildDesktop`)는 `Stack` 구조로 재작성되어 영상이 화면 최상단부터 시작하고, 스크롤하면 투명하던 상단 헤더가 불투명(solid)으로 전환되는 방식으로 바뀜.
- 다만 **모바일(`_buildMobile`)은 이 작업에서 손대지 않아** 여전히 구식 구조(고정 `AppBar`+STUDY BOX 큰 타이틀, 별도 아이콘 탭바+삼선 메뉴)를 그대로 쓰고 있었음.

이 세션 시작 시 `git pull`로 위 변경사항을 받아왔고, 사용자가 실제 화면에서 확인한 결과 두 가지 문제를 지적함.

## 문제 1: 모바일에서 영상이 화면 끝까지 안 덮임
모바일이 PC와 다른 레이아웃을 쓰고 있어서, 영상(`VideoStripBanner`)이 고정 AppBar(84px)+아이콘 탭바(46px) **아래**에서 시작함 → 화면 맨 위 가장자리를 덮지 못하고 있었음.

## 문제 2: PC에서 숨긴 메뉴(자격증/공지사항/FAQ)가 모바일에서는 계속 보임
PC는 영상 위 오버레이 메뉴(`_HeroNavRow`)에서 자격증/공지사항/FAQ를 이미 숨기고 홈/회원가입/로그인/마이페이지만 노출하도록 되어 있었지만, 모바일의 구식 아이콘 탭바는 필터링 없이 홈/자격증/공지사항/FAQ 4개를 그대로 보여주고 있어 두 플랫폼 간 메뉴 구성이 어긋나 있었음.

## 수정 내용 (`app/lib/screens/root_screen.dart` 전면 재작성)

1. **`_buildDesktop`/`_buildMobile`을 하나의 `build()`로 통합.** PC/모바일 차이는 이제 오직 두 가지뿐:
   - 콘텐츠 최대 폭(`maxWidth`): PC 1200, 모바일 `double.infinity`(제한 없음).
   - 헤더 내부 폰트/패딩 크기(`_SiteHeader`의 `isDesktop` 파라미터로 조정).
   - 그 외 구조(영상이 화면 최상단부터 시작 → 스크롤 시 투명→불투명 헤더 전환)는 완전히 동일.
2. **`_DesktopHeader`를 공통 `_SiteHeader`로 교체.** 모바일 전용이던 `_TopNavBar`(아이콘 탭바)와 `_showMobileMoreMenu`(삼선 바텀시트)는 완전히 제거.
3. **숨김 메뉴 일관화** — `_SiteHeader`(스크롤 후 불투명 상태)도 영상 오버레이(`_HeroNavRow`, 투명 상태)와 동일하게 **홈 + 마이페이지만** 노출, 자격증/공지사항/FAQ는 PC·모바일·투명·불투명 어떤 상태에서도 상단 메뉴에 나타나지 않음.
   - ⚠️ 참고: 자격증/공지사항/FAQ 탭(`LicenseScreen`/`NoticeScreen`/`FaqScreen`) 자체는 여전히 존재하고 코드 안에서 도달 가능(탭 인덱스로 이동 가능)하지만, 현재 어떤 UI에서도 안내하는 링크가 없어 사실상 진입로가 없는 상태. 필요하면 나중에 다시 노출하거나 다른 진입점(예: 마이페이지 안에 링크)을 만들어야 함.

## 테스트 수정
- `quiz_completion_test.dart`: 기존에 "자격증" 탭을 눌러 `LicenseScreen`으로 진입하던 단계를 제거 — 이제 홈 화면 영상 히어로의 **"지금 학습 시작하기" CTA**를 눌러 바로 `CertificateMenuScreen`(가맹거래사)으로 진입하도록 수정(자격증 탭이 상단 메뉴에서 사라졌으므로).
- `widget_test.dart`: "STUDY BOX" 텍스트가 2곳(앱바+영상 오버레이)에 있다고 가정하던 기대값을, 이제 헤더가 스크롤 전에는 투명(미노출)이라는 사실을 반영해 영상 오버레이 텍스트 1곳만 확인하도록 수정(`find.textContaining('STUDY BOX')`, `findsOneWidget`).
- `flutter analyze` + `flutter test`(5개) 통과 확인 후 커밋·푸시, GitHub Actions 배포 성공 확인.

## 배포 주소
https://chj927il-arch.github.io/gamyeong-exam/

⚠️ **서비스워커 캐시 주의**: Flutter 웹은 배포 후 일반 새로고침(Ctrl+F5)으로도 옛 화면이 보일 때가 있음 — 확인은 항상 **시크릿 창**으로 할 것.

## 남은 것 (다음 세션 시작점)
- [ ] 자격증/공지사항/FAQ로 가는 UI 진입점이 현재 없음 — 필요하면 마이페이지 등에 링크 추가 검토.
- [ ] PC 세로 롤링배너, 모바일 가로 롤링배너 모두 실제 이미지 없음(placeholder) — `WORKLOG_2026-07-30.md` 저녁 세션 항목 참고.
- [ ] 경영학 과목 표지 이미지 없음(placeholder).
- [ ] 회원가입/로그인/마이페이지는 전부 준비중 스낵바만 뜨는 상태(실제 인증 기능 없음).
