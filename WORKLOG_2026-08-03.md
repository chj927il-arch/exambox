# 작업 로그 — 2026-08-03

가맹거래사 1차 시험대비 앱 "스터디박스" (gamyeong-exam) — 회사 PC에서 진행한 세션. 8/1~8/2 집 PC 작업(WORKLOG_2026-08-01.md)을 받아온 뒤 이어서 진행함.

## 1. 좋아요 버튼이 같은 category 카드끼리 같이 눌리던 버그 수정

경영학 특강 그리드는 같은 category(예: '회계') 안에 카드가 3개씩 있는데(재무제표의 이해/재고자산과 매출원가/유형자산과 감가상각), 좋아요 식별자(`likeId`)를 `subjectId+category`로만 만들어서 같은 category를 공유하는 카드들이 전부 같은 좋아요 상태를 공유하고 있었음. `LectureItem`에 `subTopic`까지 포함한 `likeId` getter를 추가해 카드마다 고유하게 만듦.

## 2. Firebase 연동 (좋아요 실시간 공유)

사용자 질문("이 좋아요는 다른 사람이 누른 것도 다 보이는 거지?")을 계기로, 지금까지 좋아요/수강후기/학습기록이 전부 메모리에만 저장돼 새로고침하면 날아가는 문제를 확인함. 우선 **좋아요 기능부터** Firebase(Firestore)로 실제 공유되도록 전환.

### 진행 과정 (사람이 직접 해야 했던 부분)
- Claude Code의 셸 도구가 실행되는 환경과 사용자가 보는 실제 데스크톱 화면이 서로 다른 머신/세션이라, `npm install -g firebase-tools`·`firebase login`·`flutterfire configure`는 전부 **사용자가 직접 본인 명령 프롬프트에서 실행**해야 했음(다음 세션에서도 같은 제약이 있을 수 있음 — 참고).
  - Windows에서 `dart pub global activate`/`npm install -g`로 설치한 실행 파일은 새 터미널의 PATH에 바로 안 잡히는 경우가 많아서, 매번 전체 경로(`"C:\Users\etoos\AppData\Local\Pub\Cache\bin\flutterfire.bat"` 등)로 실행해야 했음.
  - Firebase 프로젝트를 처음 만들 때 CLI(`firebase projects:create`)로는 "Failed to add Firebase to Google Cloud Platform project" 에러가 남 — Firebase 콘솔(console.firebase.google.com)에서 **직접 "프로젝트 추가"**로 만드니 해결됨(이후 CLI가 기존 GCP 프로젝트를 인식해서 `flutterfire configure`로 연결 성공).
  - 회사 구글 계정에 2단계 인증(MFA)이 안 되어 있어 Firebase 콘솔 접근 자체가 막혀 있었음 — 콘솔에서 2단계 인증 설정 후 진행.
  - Firestore Database(Standard, 위치 asia-northeast3/서울), Authentication의 익명(Anonymous) 로그인 제공업체를 콘솔에서 수동으로 활성화.
- Firebase 프로젝트: **studybox-gamyeong-c4176**. `app/lib/firebase_options.dart`(FlutterFire CLI 생성, 웹 전용) — 원래 `.gitignore`에 있었지만, 웹 앱의 apiKey는 공개돼도 안전한 정보(보안은 Firestore 규칙이 담당)이고 GitHub Actions/다른 PC 빌드에 반드시 필요해서 **루트 `.gitignore`에서 제외 규칙을 삭제하고 커밋함**.

### 코드 변경
- `pubspec.yaml`에 `firebase_core`/`firebase_auth`/`cloud_firestore` 추가.
- `main.dart`: `Firebase.initializeApp()` + `LikesStore.instance.init()`(익명 로그인)을 `runApp` 전에 실행.
- `likes_store.dart` 전면 재작성 — Firestore `likes` 컬렉션(문서 하나당 `likedBy`: uid 배열)을 실시간 스트림으로 구독해 카운트·내 좋아요 여부를 계산. `toggle()`은 낙관적 업데이트 후 `arrayUnion`/`arrayRemove`로 서버 반영. `LikeBadge` 등 기존 소비 코드는 `LikesStore.instance`의 동일한 동기 API(`countFor`/`isLikedByMe`/`toggle`)를 그대로 쓰므로 변경 없음.
  - Firebase가 초기화 안 된 환경(위젯 테스트)에서는 조용히 무시하도록 전부 try/catch로 감쌈 — 기존 `flutter test` 그대로 통과.
- `firestore.rules` 신규(저장소 루트) — 읽기는 전체 공개, 쓰기는 로그인(익명 포함)한 사용자가 `likedBy` 필드만 수정 가능하도록 제한. 콘솔 Rules 탭에 직접 붙여넣어 게시함(CLI 배포 아님).
- 실제 로컬 서버(`http://127.0.0.1:5959`)에서 좋아요 토글 → Firestore 콘솔에 데이터 반영까지 확인 완료.

## 확인 방법 / 주의사항
- Firebase 관련 콘솔 작업(로그인, 프로젝트 생성, MFA, Firestore/Auth 활성화)은 **사용자 본인 브라우저·터미널에서만 가능** — Claude가 대신 클릭하거나 로그인할 수 없음(원격 환경 분리 + OAuth 특성).
- `flutter analyze`/`flutter test` 전부 통과 확인 후 커밋.
- 이 세션부터 계속: **매 작업 후 확인 없이 자동 커밋·푸시·배포**.

## 3. 댓글 기능 추가 (Firestore, 좋아요와 동일 구조)

- `comments/{cardId}/items` 서브컬렉션에 저장, 좋아요와 동일하게 익명 로그인 기반으로 실시간 공유(새로고침해도 유지, 다른 사용자에게도 보임).
- 좋아요 배지 옆에 댓글 배지(말풍선+개수) 추가 — `EngagementBadges` 위젯으로 좋아요+댓글을 묶어서 카드마다 하나만 붙이면 되게 함. 아이콘은 처음엔 `chat_bubble_rounded`였다가 사용자 요청으로 인스타그램 스타일 `mode_comment_outlined`(둥근 말풍선)로 변경.
- `firestore.rules`에 comments 규칙 추가(읽기 공개, 작성만 허용, 수정·삭제 불가).
- 데일리 OX퀴즈, 타임어택 카드에도 좋아요·댓글 반영.

## 4. 저장소 이름 변경 (gamyeong-exam → exambox)

- `gh repo rename`으로 GitHub 저장소명 변경, 로컬 `origin` remote URL 갱신.
- **배포 워크플로우의 `--base-href`도 함께 수정해야 함**(안 하면 배포 후 에셋 경로가 깨짐) — `.github/workflows/deploy.yml`의 `flutter build web --base-href /exambox/`로 수정.
- 폴더명(`C:\gamyeong-exam`)과 Flutter 프로젝트명(`gamyeong_exam`)은 그대로 유지 — 저장소 이름만 바뀜.
- 새 주소: https://chj927il-arch.github.io/exambox/ (기존 gamyeong-exam 링크는 GitHub이 자동 리다이렉트).

## 5. 마이페이지 학습리포트 초기화

- 연속학습일(streak)·최근 7일 학습활동 차트·과목별 진행률이 전부 **가짜 데모 숫자**(연속 3일, 랜덤 활동량, 62%/35%/8% 등)를 보여주고 있어서 오해 소지가 있었음 → 전부 0(초기 상태)으로 되돌림. 주간활동 차트는 오늘 칸만 실제 `UserProgress` 기반 학습시간으로 채움.
- 실제 로그인+서버 저장이 붙기 전까지는 이 상태 유지, 나중에 진짜 값으로 교체 예정.

## 6. 민법 특강 3개 챕터 신설 + 문제 확장

- 사용자에게 "민법특강에 추가하면 좋을 게 뭐가 있을까" 물어봤을 때 제안한 것 중 **소멸시효/채무불이행과 손해배상/계약의 해제·해지** 3개를 실제로 추가.
- 처음엔 12문항씩 작성했다가, "30문항으로 늘려줘" 요청으로 확장. 이 과정에서 civilLawLectures의 `_LectureGrid`가 `imageAsset!`(non-null assertion)을 강제하고 있어서 이미지 없는 신규 카드를 넣으면 크래시 나는 걸 발견 → `IconLectureCard`를 공개 위젯으로 바꿔서 `_LectureGrid`를 `_IconLectureGrid`로 통합(경영학·경제법 특강과 동일한 패턴으로 정리).
- ⚠️ **소멸시효 확장 시 실수** — 처음에 신규 30문항을 통째로 추가했다가, 알고 보니 기존 "학습하기" 챕터별 문제풀이 쪽에 이미 소멸시효 문제 20개가 있어서 합계 50개가 되어버림. 사용자가 "토탈 30문제씩이야"라고 정정해줘서, 기존 20개 + 신규 10개만 남기고 20개를 삭제해 정확히 30개로 맞춤. **문제 은행에 새 category로 문항을 추가할 때는 먼저 `grep -c "category: '이름'"`으로 기존 문항 수부터 확인할 것.**

## 7. 경영학 특강에 이미 존재하던 문제풀 4개 카드화 시도 → 되돌림

- 조직행동과인사관리/재무관리/생산관리/경영정보시스템은 이미 문제(각 24~40문항)가 있는데 홈 화면 카드가 없어서 접근 불가능하다는 걸 발견하고 카드 4개를 추가했었음.
- 사용자가 "특강은 문제풀이에 있는 거 그대로 연결하면 안 돼, 특강은 빈출 유형 챕터를 별도로 공부하는 것"이라며 반려 → **4개 카드 전부 제거**. "특강"은 단순 카테고리 연결이 아니라 별도로 큐레이션된 심화 콘텐츠여야 한다는 원칙을 재확인함.

## 8. 과목별 학습(SubjectInfoScreen)에 챕터별 핵심요약정리 + 문제풀이 버튼

- 출제경향·시험통계 목록의 각 챕터 항목에 **"핵심요약"**(손글씨 노트 스타일, 기존 특강용 `LectureIntroScreen` 재사용) + **"풀기"**(그 챕터만 필터링한 QuizScreen) 버튼 2개 추가.
- `chapter_summaries.dart` 신규 — (subjectId, topic) 조합으로 요약 조회. 경제법 30개·민법 10개·경영학 6개 챕터, **총 46개 전 챕터**에 실제 핵심요약 내용 작성 완료.
- 기존엔 상단에 "문제풀이 시작" 버튼 하나로 전체 문제가 뭉뚱그려져 있었는데, 챕터별로 쪼개짐. 실전모의고사는 홈 화면에 별도 카드가 생겼으므로 SubjectInfoScreen의 "실전모의고사 응시" 버튼은 중복이라 제거.

## 9. 자잘한 UI 수정들

- 데일리 OX퀴즈 목록의 "가맹거래사" 필터 박스 — 선택지가 하나뿐일 때(한능검 비활성 상태) 의미 없어서 숨김 처리.
- PC에서 특강 그리드를 마우스로 드래그해도 좌우 스크롤되도록 수정(`HScrollList`의 `ScrollBehavior`에 `PointerDeviceKind.mouse` 추가 — 기본값은 터치/스타일러스만 허용해서 카드 많아진 민법 특강 등에서 마우스 드래그가 안 먹혔음).
- 상단 메뉴바("홈" 등) 텍스트 크기 확대(PC 14→16, 모바일 12.5→14) — "홈 버튼이 잘 안 보인다"는 피드백 반영.
- 타임어택 카드에 표지 이미지 반영(사용자 제작 이미지, 이후 고해상도 버전으로 2차 교체) + 카드 위 "1분 제한"/"타임어택" 텍스트 오버레이 제거(이미지 자체에 이미 문구가 있어 중복).

## 10. "함정 피하기" 그리드카드 신설 (경제법 특강 아래, 3과목 혼합 30문항)

- 처음 요청은 "고난도 문제풀이 시즌1"이었으나, 사용자가 "고난도 말고 함정 피하기로, 컨셉은 자주 틀리고 헷갈리고 용어가 비슷한 문제 공략"으로 정정 → 전체 재작명(`category`, 파일 내 id 접두사 `gen_hard_`→`gen_trap_`, 카드/스타일 키 등)함.
- 3과목(경제법·민법·경영학)에서 서로 헷갈리기 쉬운 유사 개념·제도를 비교·구분하는 문제 30개(과목별 10개) 신규 작성.
- `QuizScreen`에 `crossSubjectIds` 옵션 추가 — 여러 과목 문제를 하나의 세션으로 섞어서 풀되, 학습기록(`UserProgress`)은 문제별 실제 과목(subjectId/subjectName)으로 정확히 저장되도록 구현(카드 자체의 `subjectId`는 스타일/색상 용도로만 별도 사용 — `trap_quiz_season1`).
- 카드 이미지는 사용자가 여러 차례 다시 만들어 보내준 "오답주의"/"함정주의" 이미지로 최종 반영(다운로드 폴더 파일을 그때그때 복사해서 교체).

## 11. 문제풀이 화면에 전체 문제 목록/바로가기 기능

- 상단 바에 목록 아이콘 버튼 추가 — 누르면 이번 세션의 전체 문제를 **번호 + 문제 본문(stem) + 유형(세부유형/카테고리)**과 함께 보여주는 바텀시트가 뜨고, 탭하면 그 문제로 바로 이동.
- 처음엔 유형 이름만 보여줬다가 "문제 제목이 나오게 해줘야지" 피드백으로 문제 본문을 제목으로 바꾸고 유형은 그 아래 작은 글씨로 이동.
- 이미 푼 문제는 정답/오답 아이콘이 함께 표시되고(`_QuestionScreenState`에 `_results: List<bool?>` 추가), 목록을 통해 다시 봐도 학습기록(통계)은 중복 반영되지 않도록 처리.

## 확인 방법 / 주의사항 (누적)
- Firebase 콘솔 작업은 여전히 사용자 본인만 가능(환경 분리).
- 문제 은행에 새 category 추가 시 `grep -c "category: '이름'"`으로 기존 문항 수 먼저 확인(중복 카운트 실수 방지, 6번 항목 참고).
- 저장소 이름 등 배포 경로에 영향 주는 변경은 `base-href`도 함께 확인.
- `flutter analyze`/`flutter test` 전부 통과 확인 후 커밋 → 매 작업 후 확인 없이 자동 커밋·푸시·배포(계속 유지).

## 남은 것 (다음 세션 시작점)
- [ ] 수강후기·학습기록(UserProgress)도 아직 메모리에만 저장 — Firestore로 옮기는 작업은 좋아요·댓글 다음 순서로 남아있음.
- [ ] 로그인/회원가입 UI는 여전히 준비중 스낵바 — 실제 이메일/구글 로그인(Firebase Auth)은 아직 미구현. 사용자가 "생각 정리 좀 하고" 보류한 상태(마이페이지 학습리포트 실 데이터화, 약점 챕터 AI 유사문제 생성 등 큰 그림과 함께 다시 논의 예정).
- [ ] Firestore 보안 규칙이 `likes`/`comments` 컬렉션만 다룸 — 다른 컬렉션 추가 시 규칙도 함께 확장 필요.
- [ ] 경영학 표지 이미지 없음(과목별 학습 카드), 자격증/공지사항/FAQ 진입점 없음 — 계속 남아있음.
- [ ] "함정 피하기"·"실전모의고사"·"타임어택" 등은 전부 "시즌1"만 있음 — 콘텐츠 소진되면 시즌2 등 추가 검토.
