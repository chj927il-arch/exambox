# 작업 로그 — 2026-08-05

가맹거래사 1차 시험대비 앱 "스터디박스"(exambox) 세션. 8/4 세션(WORKLOG_2026-08-04.md) 마지막에 기록한 시점 이후 같은 대화에서 이어서 진행한 작업.

## 1. 데스크톱 2분할 화면 정렬 반복 수정

학습하기(특강·함정피하기)의 2분할 화면에서 "정답/오답 피드백이 시작되는 줄이 안 맞는다"는 스크린샷 피드백을 몇 차례 더 받으며 다듬음.
- 처음엔 배지(유형·기출연도) 유무 차이가 원인이라 2분할일 때는 배지를 숨김.
- 그래도 안 맞아서 `TextPainter`로 지문 실제 렌더링 높이를 계산해 짧은 쪽 **지문과 보기 사이**에 빈 공간을 채우는 방식을 시도했으나, 사용자가 "그럴거면 해설을 더 아래에서 여유있게 보여주면 되지 않냐"고 정확한 방향을 제시함.
- 최종적으로 **지문+보기 전체 높이**(TextPainter로 지문, 추정 로직으로 보기까지 계산)를 좌우 비교해서, 짧은 쪽 **피드백 박스 바로 위**에만 차이만큼 빈 공간을 넣는 방식으로 정리 — 문제 자체 레이아웃은 건드리지 않고 피드백 시작 줄만 맞춤.

## 2. 단권화 노트 화면 신설

해설 옆 📑 버튼으로 "단권화 저장"은 되고 있었는데 저장한 걸 보는 화면이 아예 없던 반쪽짜리 기능을 발견 → `screens/compiled_note_screen.dart` 신규(오답노트와 동일 패턴, Scaffold+AppBar로 뒤로가기 가능하게 함), 마이페이지 메뉴에 추가. 해설 안 "오답 노트" 라벨이 이 기능과 헷갈려서 **"핵심 포인트"로 이름 변경**(quiz_screen.dart 2곳 + mock_exam_screen.dart + full_mock_exam_screen.dart), 표시 방식도 뱃지 나열 → 불릿 목록으로 변경.

## 3. 한능검(한국사능력검정시험) 재오픈

7/30에 가맹거래사 전용 앱으로 개편하며 꺼두었던 `kKoreanHistoryEnabled` 플래그를 다시 `true`로 전환.
- "과목별 학습" 그리드에 한능검 카드 노출(기존에 이미 만들어져 있던 `_KoreanHistorySubjectCard`가 플래그만 기다리고 있었음).
- "한능검 특강" → "부족한 단원만 공부한다_한능검"으로 이름 변경(민법·경영학·경제법과 동일 네이밍), 이후 사용자 요청으로 "틀리기 쉬운 문제" 섹션보다 위로 위치 이동.
- 함정 피하기(틀리기 쉬운 문제)·타임어택의 과목 풀에 `korean_history` 추가.
  - **함정 피하기**는 `category: '함정 피하기'`로 필터링해서 문제를 모으는데 한능검 문제가 아예 없어서 → 헷갈리기 쉬운 한국사 항목(광종·성종, 서희·강감찬, 갑신정변·갑오개혁, 4·19·5·18·6월항쟁 등) 10문제를 새로 작성.
  - **타임어택**은 원래 과목 필터 없이 전체 문제 중 랜덤이라 플래그 추가만으로 이미 한능검이 섞여 나오고 있었음(추가 문제 작성 불필요).
- 학습하기(특강) 화면에 "지금 몇 과목 문제인지" 구분하는 상단 과목 탭 추가:
  - 학습하기(quiz_screen.dart, crossSubjectIds 사용 시)는 **탭하면 그 과목 첫 문제로 이동**하는 인터랙티브 탭.
  - 타임어택은 처음엔 장식용(탭 안 됨)으로 만들었다가, 사용자가 "탭이 안 눌린다"고 지적 → **탭하면 그 과목 문제만 나오게 필터링**(다시 누르면 해제)하는 기능으로 수정.

## 4. 한능검 이미지 문제 수정

- "과목별 학습" 한능검 표지(`subject_cover_korean_history.png`)가 가로형(1402×1122)인데 카드는 세로형(152×216)이라 옆이 크게 잘리던 문제 → PowerShell(`System.Drawing`)로 제목 위주 세로 크롭(795×1122)해서 다른 과목 카드와 비율을 맞춤.
- 근현대사 특강 표지만 JPEG(손실 압축)이고 나머지는 전부 PNG(무손실)였음 → PNG로 재저장해서 압축 아티팩트 제거.

## 5. 한능검 특강(고려·조선·근현대사) 문제 30개씩 확장

기존에 24문제씩(고려/조선전기/일제강점기와 현대)만 있던 걸 사용자 요청으로 각 6문제씩 새로 작성해 **30문제**로 맞춤.
- 고려: 삼별초 항쟁, 원 간섭기, 공민왕의 반원 개혁, 전민변정도감, 신진사대부, 위화도 회군(고려 후기 정치 변동 묶음).
- 조선전기: 광해군 중립외교, 인조반정, 이괄의 난, 정묘·병자호란, 대동법(광해군의 정책과 호란 묶음).
- 일제강점기와 현대: 토지 조사 사업, 산미 증식 계획, 민족 말살 통치, 4·19·5·18·유신 체제(일제의 경제 수탈과 현대사 주요 사건 묶음).

## 6. 한능검 특강 필기노트(핵심 포인트) 신규 작성

특강 카드를 누르면 나오는 손글씨 노트 화면(`LectureIntroScreen`)의 keyPoints가 고려/조선/근현대사 전부 `['추후 업데이트 예정']` 플레이스홀더 상태였던 걸 발견 → 각 30문제에서 다루는 핵심 내용을 빠짐없이 반영해 고려 24개·조선 15개·근현대사 20개 항목으로 상세 작성(민법·경제법·경영학 특강보다 항목 수를 훨씬 많이 잡아 달라는 요청 반영).

## 7. 홈 화면 누적 방문 수 표시 → 다시 숨김

- `data/visit_counter.dart` 신규 — Firestore(`stats/visits` 문서의 count 필드)를 앱 실행마다 `FieldValue.increment(1)`로 늘리고 그 값을 홈 화면에 표시.
- `firestore.rules`에 `stats/visits` 규칙 추가(읽기 공개, 로그인한 사용자가 count 필드만 쓰기 가능) 후 `firebase deploy --only firestore:rules`로 배포.
- 배포 후 실제 프로덕션 사이트 방문으로 카운트가 1→2로 정상 증가하는 것까지 확인.
- 사용자 요청으로 배지 노출은 껐지만(`_kShowVisitCounter = false`), 카운트 기록 자체는 계속 유지 — 나중에 플래그만 켜면 다시 보임.

## 확인 방법 / 주의사항

- 이 세션도 계속: **매 작업 후 확인 없이 자동 커밋·푸시·배포**, 매번 `flutter analyze`+`flutter test` 통과 확인 후 커밋.
- 이번 세션에서 로컬 개발 서버(`flutter run -d web-server`) 미리보기가 여러 차례 응답 없이 멈추는 현상이 있었음(DDC 컴파일이 큰 `sample_questions.dart` 때문에 오래 걸리거나 이 환경 특유의 문제로 추정) — 이럴 때는 로컬 대신 **배포된 프로덕션 사이트**(`https://chj927il-arch.github.io/exambox/`)에 직접 접속해 콘솔 에러·동작을 확인하는 방식으로 우회함. `flutter analyze`/`flutter test`가 전부 통과한 상태에서의 배포이므로 코드 자체의 문제는 아니었음.
- 방문 수 카운터처럼 Firestore에 새 컬렉션/문서를 추가할 때는 `firestore.rules`에 규칙을 추가하고 **`firebase deploy --only firestore:rules`**로 별도 배포해야 실제로 쓰기가 허용된다(콘솔 Rules 탭 수동 붙여넣기 없이 CLI로 가능).
- 보류 중: 핵심 포인트(키워드+설명) 전체 재작성은 대상 문제가 1,842개라 사용자 요청으로 보류 상태.
