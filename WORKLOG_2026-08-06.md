# 작업 로그 — 2026-08-06

가맹거래사 1차 시험대비 앱 "스터디박스"(exambox) 세션. 8/5 세션(WORKLOG_2026-08-05.md) 이어서 진행.

## 1. 블로그 경유 접속 게이트 신설

사용자가 운영 중인 네이버 블로그("슬기로운 수험생활")로 문제은행 사이트 방문자를 유도하고 싶다는 요청 — "블로그 포스팅의 바로가기를 눌러야만 입장 가능, 새로고침은 괜찮지만 창을 닫고 다시 열면 다시 블로그를 거쳐야 함"이라는 구체적인 동작을 요청받아 구현.

- 정적 호스팅(GitHub Pages)이라 서버 단 리퍼러 체크는 불가능 → **클라이언트 쪽 `sessionStorage` + URL 파라미터(`?ref=blog`)** 조합으로 구현.
  - URL에 `?ref=blog`가 있으면 통과시키고 `sessionStorage`에 기록.
  - `sessionStorage`는 새로고침해도 유지되지만 탭/창을 완전히 닫으면 사라지므로, 요청한 동작과 정확히 일치.
- **`flutter test`가 깨지지 않도록 조건부 임포트 패턴 적용**: `dart:html`은 VM 테스트 러너(`flutter test`)에서 컴파일 자체가 안 되는 웹 전용 라이브러리라, `blog_gate.dart`(공개 인터페이스) / `blog_gate_web.dart`(실제 `dart:html` 구현) / `blog_gate_stub.dart`(테스트용 항상 통과) 3개 파일로 분리하고 `export ... if (dart.library.html) ...`로 웹에서만 진짜 로직이 붙게 함. widget_test.dart가 main.dart를 직접 임포트하고 있어서 이 처리가 없으면 테스트가 컴파일 에러로 깨짐.
- `screens/blog_gate_screen.dart` 신규 — 안내 화면. 사용자가 첨부한 "슬기로운 수험생활" 블로그 배너 이미지를 넣고, "문제은행 사이트로 이동하기 위해서는 '슬기로운 수험생활' 블로그를 통해 접속해주세요" 문구 + 블로그로 이동 버튼(텍스트 길이에 맞는 필 모양, 흰 글씨).
- `config/feature_flags.dart`에 `kBlogGateEnabled` 플래그 추가(끄면 원래대로 누구나 바로 입장).
- **실제 프로덕션 사이트에서 동작 검증**: `?ref=blog`로 입장 → 세션 저장 확인, 세션 삭제 후 재방문 → 안내 화면 재노출까지 직접 확인함(로컬 개발 서버 미리보기가 이 세션 내내 불안정해서, 배포된 프로덕션 URL에 직접 접속해 콘솔 에러 유무·`sessionStorage` 상태를 스크립트로 확인하는 방식으로 검증).
- 블로그 주소는 우선 블로그 메인(`https://blog.naver.com/franchisestory`)으로 연결해둠 — 사용자가 문제은행 소개 포스팅을 올리면 그 포스팅 주소로 교체 예정.
- ⚠️ 사용자가 링크 클릭 후 게이트가 안 뜬다고 문의 → 원인은 버그가 아니라, 제가 안내에 사용한 링크 자체에 `?ref=blog`가 이미 붙어 있어서 정상적으로 통과된 것이었음. **이후로는 사용자에게 사이트 주소를 안내할 때 항상 `?ref=blog` 없는 순수 주소만 전달하기로 함**(게이트 테스트가 매번 정확히 되도록).

## 2. 오늘/누적 방문자 수 → 상단 헤더로 이동

- 기존에 홈 화면에 숨겨뒀던 누적 방문 수 배지를 정리하고, `data/visit_counter.dart`에 **오늘 방문자 수**를 추가로 기록하도록 확장 — Firestore `stats/visits`(누적) + `stats/visits/daily/{yyyy-MM-dd}`(오늘, 자정 지나면 새 문서로 자연히 리셋) 두 곳에 동시 기록.
- `firestore.rules`에 `daily` 서브컬렉션 규칙 추가 후 `firebase deploy --only firestore:rules`로 배포.
- 사용자 요청으로 **상단 헤더 우측의 회원가입/로그인/마이페이지 링크를 숨기고, 그 자리에 "오늘 방문자 / 누적 방문자" 숫자를 표시**하도록 `root_screen.dart`의 `_SiteHeader` 교체(홈 화면에 있던 개별 배지 위젯은 정리해서 제거, 표시 위치를 헤더 하나로 통일).
- 배포 후 오늘 날짜 Firestore 문서에 정상적으로 카운트 쌓이는 것까지 확인.

## 3. 블로그 게이트 강화 — URL 파라미터 → document.referrer 기반으로 전환

`?ref=blog` 파라미터 방식은 그 주소를 한 번이라도 안 사람이 북마크해두면 영구적으로 블로그를 안 거쳐도 되는 구조적 한계가 있었음(사용자가 "이 주소를 알고 있어도 매번 블로그를 통해 접속하게 해야 돼"라고 명확히 요청). → URL 파라미터 검사를 없애고, **`document.referrer`(브라우저가 실제로 보내주는 "이전 페이지 주소")가 `blog.naver.com`인지**로 판단하도록 `blog_gate_web.dart` 교체.

- 이제 블로그 포스팅에는 파라미터 없는 순수 주소(`https://chj927il-arch.github.io/exambox/`)만 걸어두면 됨 — 브라우저가 알아서 리퍼러를 실어 보내줌.
- `?ref=blog`를 직접 주소창에 입력하거나 북마크해서 들어가도 이제는 (블로그 페이지를 거치지 않았으므로 referrer가 없어) 안내 화면이 뜨는 것까지 프로덕션에서 직접 확인함.
- sessionStorage 기반의 "탭을 닫기 전까지는 유지" 동작은 그대로 유지 — 시간(하루 등) 기준이 아니라 "그 탭/창이 열려있는지"가 기준이라는 점을 사용자에게 반복해서 설명함.
- 참고: 카카오톡 인앱 브라우저 등 일부 특수 환경에서는 referrer가 제대로 전달되지 않을 수 있음(사용자에게 안내해둠, 실제 이슈 보고되면 추가 대응 필요).

## 확인 방법 / 주의사항

- **이번 세션 핵심 교훈**: `flutter test`가 쓰는 VM 테스트 러너에서는 `dart:html` 같은 웹 전용 라이브러리를 import할 수 없다. 웹에서만 필요한 기능(브라우저 API 등)을 추가할 때는 항상 `stub.dart`(테스트/비웹용) + `_web.dart`(dart:html 실제 구현) + 조건부 export 패턴을 써야 `flutter analyze`/`flutter test`가 깨지지 않는다.
- 로컬 개발 서버(`flutter run -d web-server`) 미리보기가 이번 세션도 여러 차례 응답 없이 멈춰서, 배포된 프로덕션 사이트에 직접 접속해 `javascript_tool`로 `sessionStorage`·마운트 여부·콘솔 에러를 확인하는 방식으로 검증을 대체함.
- 매 작업 후 확인 없이 자동 커밋·푸시·배포, 매번 `flutter analyze`+`flutter test` 통과 확인 후 커밋하는 원칙 계속 유지.
- 블로그 게이트(`kBlogGateEnabled`)는 지금 켜져 있는 상태 — 마음에 안 들면 언제든 끌 수 있음. 사용자에게 사이트 주소 안내 시 `?ref=blog` 파라미터를 붙이지 않도록 주의할 것(게이트 우회를 의도치 않게 만들어버림).
