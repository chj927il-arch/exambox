# 작업 로그 — 2026-08-01

가맹거래사 1차 시험대비 앱 "스터디박스" (gamyeong-exam) — 하루 동안 이어진 세션. 전날(7/31) 작업은 `WORKLOG_2026-07-31.md` 참고.

## 1. 메인 화면 인트로 영상 삭제 → 학습 시작 배너로 대체

- 홈 화면 최상단 영상 히어로(`VideoStripBanner`) 완전 삭제, `video_player` 패키지 의존성/영상 에셋 파일까지 정리
- 영상이 유일한 학습 진입 CTA였어서, 같은 자리에 "지금 학습 시작하기" 텍스트 배너(`_StartStudyBanner`)로 대체 → 가맹거래사 학습 화면으로 바로 이동
- `root_screen.dart`의 헤더를 항상 고정 표시로 단순화 (영상 위 투명→불투명 스크롤 전환 로직 전체 제거)

## 2. 한국사능력검정시험(한능검) 노출 토글 반복

- 처음에 `kKoreanHistoryEnabled`를 `true`로 되돌렸으나, 영상 삭제로 홈 화면이 개편되면서 실제로는 화면에 보이는 진입점이 없다는 것을 발견
- 홈 화면 "과목별 학습" 행 끝에 한국사능력검정 포스터 카드 추가, "민법 특강" 아래에 "한능검 특강" 그리드(고려/조선/근현대사, 예전에 만들어둔 표지 이미지 재사용) 신설
- 이후 사용자가 "한능검 관련 싹 숨겨줘"라고 요청 → `kKoreanHistoryEnabled`를 다시 `false`로. 위 신규 항목들이 전부 이 플래그 하나로 조건부 렌더링되어 있어 한 번에 다시 숨겨짐

## 2-1. 경영학 기출 분석 + 유사문제 대량 생성

- 사용자 바탕화면의 `가맹거래사기출` 폴더(2016~2026년 11개년 1차 시험 문제지 PDF)를 `pdftotext`로 추출해 경영학(제3과목, 81~120번) 부분 분석
  - 2016~2018년은 PDF 폰트 인코딩 문제로 텍스트가 깨져 제외, 2020년은 스캔 이미지라 텍스트 추출 자체가 안 되어 제외 → 2019·2021~2026년 7개년(228문항 분류 가능)만 분석
- 실제 출제 영역이 기존 4분류(재무관리/마케팅/조직행동/생산관리)보다 세분화되어 있음을 확인 → **회계·경영정보시스템**을 추가해 6개 챕터로 재구성 (`exam_subject.dart`, `topic_stats.dart`)
- 출제 비중 1위 "회계" 챕터부터 시작해, 이어지는 요청에 따라 **경영학 6개 챕터(회계/마케팅/조직행동·인사관리/재무관리/생산관리/경영정보시스템) 전부 144문항** 신규 작성 (`sample_questions.dart`)

## 3. 프로모션 배너 이미지 화질 개선 (2단계)

1. 1차: CanvasKit(웹)이 큰 원본 이미지를 카드 표시 크기보다 훨씬 크게 그리며 스케일링을 잘못 잡던 문제 → `LayoutBuilder` + `ResizeImage`(devicePixelRatio 반영 디코드 크기 지정)를 PC 병렬배너/모바일 롤링배너 양쪽에 적용
2. 시크릿 창에서도 여전히 흐릿하다는 피드백 → 원본이 2100~2200px로 너무 커서 표시 크기(카드당 약 380px)까지 5~6배 다운스케일되는 과정에서 화질이 떨어지는 것으로 진단. PowerShell `System.Drawing`(HighQualityBicubic)으로 원본 에셋 자체를 1200px 폭으로 사전 리사이즈해 다운스케일 비율을 절반(약 3배)으로 축소 → 글씨 가독성 유지 확인

## 4. 경영학·경제법 특강 그리드 신설 (표지 이미지 없이 아이콘 카드로)

- 민법 특강 아래에 "경영학 특강"(회계·마케팅 위주 6개), "경제법 특강"(기출 비중 상위 + 실무 연관성 고려 6개) 그리드 추가
- 표지 이미지가 없어 `TintedPoster` 대신 `subjectStyleOf` 기반 그라데이션+아이콘 카드(`_IconLectureCard`) 신규 제작
- `LectureItem.imageAsset`를 nullable로 변경하고 `subTopic`·`styleKey` 필드 추가, `QuizScreen`/`LectureIntroScreen`에 `subTopic` 필터를 지원하도록 확장(같은 category 안에서도 세부 주제 하나만 골라 학습 가능)
- 이후 요청에 따라 **경영학 특강 6개 챕터를 각 30문항으로 확장**(기존 6문항 → 30문항, 총 144문항 신규), **경제법 특강 6개 챕터도 전부 30문항으로 통일**(24→30이었던 3개 챕터에 각 6문항씩 추가)

## 5. 버그 수정 — 민법 "동시이행항변권" 특강 문제 0개

- 원인: 특강 카드가 `category: '동시이행항변권'`을 가리키고 있었는데, 실제 문제는 `계약총론(성립·동시이행·해제 등)` 카테고리 안에 2문항만 섞여 있어 필터링 결과가 0개였음
- 다른 민법 특강 카드와 동일하게 독립된 전용 카테고리로 분리해 30문항 신규 작성, 카드의 핵심 개념 요약도 실제 내용으로 교체

## 6. 앱 시작 표지(스플래시) 화면 완전 제거

- `main.dart`가 `SplashScreen` 대신 `RootScreen`을 곧바로 `home`으로 사용하도록 변경 — 1초 지연 + 페이드 전환 애니메이션 전부 제거
- `splash_screen.dart` 파일과 `splash_cover.png` 에셋 삭제
- PC/모바일 모두 `RootScreen`이 폭에 따라 자체적으로 레이아웃을 분기하므로 플랫폼별 추가 처리 불필요
- 테스트도 스플래시 대기 로직 제거하고 단순화

## 확인 방법 / 주의사항

- 매 커밋마다 GitHub Actions가 자동으로 `flutter build web` 후 배포 (1~2분 소요) — 이 세션부터 **매 작업 후 확인 없이 자동 커밋·푸시·배포**하기로 함(사용자 요청, 메모리에도 기록됨)
- 배포 확인은 항상 **시크릿 창**으로 — 일반 창은 서비스워커가 이전 빌드를 캐싱해서 보여주는 경우가 많음
- `flutter analyze` / `flutter test` 전부 통과 확인 후 커밋하는 것을 원칙으로 진행함

## 남은 것 (다음 세션 시작점)

- [ ] 조선/근현대사 한능검 특강 카드는 세부 시대(조선전기·후기, 근대·일제강점기)로 아직 분리되지 않음 — 필요 시 세분화 검토
- [ ] 경영학 특강 카드들도 표지 이미지가 없어 아이콘 카드 상태 — 필요 시 실제 이미지 제작해 `imageAsset`만 채우면 사진 카드로 전환 가능
- [ ] 회원가입/로그인/마이페이지는 여전히 준비중 스낵바만 뜨는 상태(실제 인증 기능 없음)
- [ ] 경영학 표지 이미지(과목별 학습 카드) 없음(placeholder)
