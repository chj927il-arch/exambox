# 작업 로그 — 2026-08-04

가맹거래사 1차 시험대비 앱 "스터디박스"(exambox) 세션. 8/3 세션(WORKLOG_2026-08-03.md) 이어서 진행.

## 1. 민법·경영학 특강 표지 이미지 5개 반영

사용자가 첨부한 Downloads 폴더의 새 이미지 5장을 제목으로 매칭해 `lecture_data.dart`의 `imageAsset`으로 연결.
- 소멸시효 / 채무불이행과 손해배상 / 계약의 해제·해지 (민법)
- 유형자산과 감가상각 (경영학)
- 재판매가격유지행위 (경제법)

파일명은 `lecture_cover_{styleKey}.png` 규칙으로 통일, `pubspec.yaml` assets에 등록.

## 2. 이메일 회원가입/로그인 + 학습리포트 실데이터 연동

기존에는 마이페이지 학습리포트(연속학습일·최근7일활동·과목별 취약챕터)가 전부 세션 메모리에만 있어 새로고침하면 날아갔음. 이메일 계정으로 로그인하면 Firestore에 저장되어 기기를 바꿔도 이어지도록 구현.

- `services/auth_service.dart` 신규 — 이메일/비밀번호 회원가입·로그인·로그아웃. 회원가입은 기존 익명 세션에 `linkWithCredential`로 연결해 uid를 유지(그래야 가입 전 좋아요 등이 안 끊김).
- `screens/login_screen.dart` 신규 — 로그인/회원가입 토글 폼.
- `data/user_progress.dart` 대폭 개편 — `attachUser(uid)`로 로그인 시 Firestore(`users/{uid}` 문서 + `users/{uid}/daily/{yyyy-MM-dd}` 서브컬렉션)에서 불러오고, 문제 풀 때마다 `FieldValue.increment`로 실시간 동기화. 로그아웃 시 게스트 화면에 이전 계정 데이터가 남지 않도록 초기화.
- `firestore.rules`에 `users/{uid}` 규칙 추가(본인 uid만 read/write), `firebase.json`/`.firebaserc` 신규 생성 후 `firebase deploy --only firestore:rules`로 배포.
- 마이페이지: 비로그인 시 "로그인" 버튼 배너, 로그인 시 이메일 표시 + 로그아웃 메뉴.
- ⚠️ Firebase 콘솔의 Authentication에서 "이메일/비밀번호" 로그인 방식이 켜져 있어야 실제 가입이 동작함(사용자에게 확인 요청함, 미확인 상태).
- Firebase 무료 요금제(Spark) 한도 안에서 충분히 운영 가능하다고 안내(질문에 대한 답변, 코드 변경 없음).

## 3. 문제풀이 화면을 실제 시험지 스타일로 전면 개편

사용자가 실제 기출문제 스크린샷을 첨부하며 "이런 느낌으로" 요청 → 여러 차례 피드백을 받으며 반복 수정.

- 보기 라벨을 A/B/C/D/E → ①②③④⑤로 변경 (`quiz_screen.dart`, `mock_exam_screen.dart`, `full_mock_exam_screen.dart` 전체).
- 처음엔 검은 테두리 박스로 문제를 감쌌으나 "실제 시험지는 박스가 없다"는 피드백으로 박스 제거, 배경 위에 바로 배치.
- 지문·보기 줄바꿈 시 문항번호(①~⑤) 밑이 아니라 텍스트 첫 글자 위치로 정렬되는 행잉 인덴트 적용.
- 해설은 정답/오답 확인 직후 바로 보이지 않고 "해설 보기" 버튼을 눌러야 펼쳐지도록 변경 — 학습하기(`quiz_screen.dart`)/모의고사 결과화면(`mock_exam_screen.dart`, `full_mock_exam_screen.dart`)/타임어택(`time_attack_screen.dart`) 전부 동일 적용.
- 해설 안의 "핵심 개념" 칩 나열 → 첨부 이미지 참고해 "오답 노트"라는 이름의 불릿 목록으로 변경했다가, 사용자가 실제 오답노트 기능과 헷갈려해서 **"핵심 포인트"로 재변경**(아래 5번 참고).

## 4. 데스크톱 2분할(좌우) 화면

브라우저 폭이 900px 이상(desktop breakpoint)이면 문제를 두 개씩 좌우로 나란히 보여주는 "시험지 펼침 면" 레이아웃 추가.

- `mock_exam_screen.dart`, `full_mock_exam_screen.dart`: pairStart(짝수 인덱스)/pairStart+1 두 문제를 `_buildQuestionPair()`로 렌더링, "이전/다음"이 2문제씩 이동.
- `quiz_screen.dart`(학습하기/특강/함정피하기): 처음엔 오른쪽을 회색 미리보기(터치 불가)로 구현했으나 "왼쪽을 풀기 전엔 오른쪽을 못 푸는 게 불편하다"는 피드백으로 **양쪽 다 독립적으로 풀 수 있게** 전면 리팩터링.
  - `_selectedIndex`/`_answered` 단일 필드를 문제별 배열(`_selected`, `_results`)로 전환 → 좌우 어느 쪽이든 순서 상관없이 답을 고를 수 있고, 각자 자기 문제 밑에 정답/오답+해설 보기가 바로 뜬다.
  - 화면 하단 "이전/다음"(2문제씩)·"학습 마치기" 버튼 추가.
- **정렬 문제 반복 수정**: 좌우 지문 길이가 달라 "①~⑤ 시작 줄"이나 "정답/오답 피드백 시작 줄"이 어긋나는 문제를 사용자가 스크린샷으로 계속 지적함 → `TextPainter`로 지문·보기의 실제 렌더링 높이를 미리 계산해서, **짧은 쪽 피드백 박스 위에** 그 차이만큼 빈 공간을 넣는 방식으로 최종 정리(문제 자체 사이에는 빈틈을 두지 않고, 정답/오답 피드백이 시작되는 줄만 좌우로 맞춤). 2분할 배지(유형·기출연도)도 좌우 유무가 달라 정렬이 깨지는 원인이었어서, 2분할 화면에서는 배지를 아예 숨김.

## 5. 단권화 노트 화면 신설 (죽어있던 기능 살림)

문제풀이 중 해설 옆 📑 버튼으로 "단권화" 저장은 이미 되고 있었는데, 저장한 걸 다시 볼 수 있는 화면이 아예 없었음(`UserProgress.compiledQuestionIds`만 있고 소비하는 화면이 없는 반쪽짜리 기능). 사용자가 "오답노트 기능이 뭐야, 저장 버튼도 없던데" 라고 물어본 걸 계기로 확인 후 신설.

- `screens/compiled_note_screen.dart` 신규 — `WrongNoteScreen`과 동일 패턴(질문 목록 + 제거 버튼), `Scaffold`+`AppBar`로 감싸 뒤로가기 가능하게 함.
- 마이페이지 메뉴에 "단권화 노트" 항목 추가(오답노트 바로 아래).
- 해설 안 "오답 노트"라는 이름이 이 기능과 헷갈려서 **"핵심 포인트"로 이름 변경**.

## 6. 과목명 표기 수정

홈 화면 "민법 특강"/"경영학 특강"/"경제법 특강" → "부족한 단원만 공부한다_민법"/"_경영학"/"_경제법" (특강이라는 단어 자체를 빼고 컨셉을 그대로 드러내는 문구로). 중간에 조사 "다"가 빠진 걸 한 번 더 수정.

## 7. 보류 — 핵심 포인트 내용 보강

키워드만 있는 "핵심 포인트"에 짧은 설명을 붙여달라는 요청이 있었으나, `keyPoints`가 붙은 문제가 1,842개라 전량 재작성은 시간이 많이 걸려 **사용자 요청으로 보류**. 다음에 이어서 하려면: 전체 재작성 / 최근 추가 특강 챕터(소멸시효·채무불이행과 손해배상·계약의 해제해지·유형자산과 감가상각·재판매가격유지행위)만 우선 / 특정 과목·챕터 지정 중 택일해서 시작하면 됨.

## 확인 방법 / 주의사항

- 이 세션도 계속: **매 작업 후 확인 없이 자동 커밋·푸시·배포**, 매번 `flutter analyze`+`flutter test` 통과 확인 후 커밋.
- 스크린샷 도구가 이 세션 환경에서 계속 안 먹혀서, 시각적 확인은 콘솔 에러 없음 + 코드 리뷰로 대체함. 실제 화면은 사용자가 매번 스크린샷을 보내줘서 그걸로 반복 교정.
