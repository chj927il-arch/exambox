# 작업 로그 — 2026-07-30

가맹거래사 1차 시험대비 앱 "스터디박스" (gamyeong-exam) — 앱 방향 재확정: **가맹거래사 1차 시험 대비 앱으로 개편**, 한국사능력검정시험(한능검) 관련 콘텐츠는 삭제가 아닌 **비노출(준비 중) 처리**.

## 배경

7/26~29 사이 STUDY BOX 특강 메뉴를 신설하면서 한능검(한국사능력검정시험) 콘텐츠(자격증 메뉴, 홈 화면 배너/특강 그리드, 데일리 OX 필터)가 함께 추가됨. 앱의 본래 목적을 가맹거래사 1차 시험 대비로 명확히 하기로 하고, 한능검 콘텐츠는 **완전히 지우지 않고** 나중에 다시 켤 수 있도록 스위치 하나로 숨김.

## 변경 내용

### `app/lib/config/feature_flags.dart` (신규)
- `const bool kKoreanHistoryEnabled = false;` 플래그 하나로 한능검 노출 여부 전체 제어.
- **다시 노출하려면 이 값을 `true`로 바꾸고 재빌드/재배포하면 됨.** 데이터·화면 코드·이미지 에셋은 전혀 건드리지 않음(전부 그대로 남아있음).

### 숨긴 지점 4곳 (모두 `kKoreanHistoryEnabled`로 조건부 렌더링)
1. `app/lib/screens/license_screen.dart` — 자격증 탭 '취업/기타' 카테고리의 '한국사능력검정' 항목을 `ready: false`로 전환(다른 준비중 자격증과 동일하게 "준비 중이에요" 토스트만 뜸).
2. `app/lib/screens/home_screen.dart`
   - 롤링배너(`STUDY BOX 콘텐츠`)의 한국사 배너 카드 숨김.
   - `_SubjectGuideCarousel`(과목 안내)의 한국사능력검정시험 표지 카드 숨김.
   - '한능검 특강' 섹션 타이틀 + `_KoreanHistoryLectureGrid`(고려/조선/근현대사 카드) 전체 숨김.
3. `app/lib/screens/daily_ox_list_screen.dart` — 데일리 OX 퀴즈 화면의 자격증 필터 칩에서 '한국사능력검정' 제거(가맹거래사만 노출).

### 건드리지 않은 것 (삭제 아님, 그대로 보존)
- `korean_history_intro_screen.dart`, `korean_history_level_screen.dart`, `lecture_intro_screen.dart` 등 화면 파일
- `topic_stats.dart`/`sample_questions.dart`/`ox_quiz_data.dart`의 한국사 데이터
- `subject_style.dart`의 `korean_history`/`korean_history_basic` 스타일 정의
- 표지·배너·특강 이미지 에셋 전부

→ 코드/데이터는 그대로 있고 진입 경로만 막혀 있는 상태. `flutter analyze` 통과 확인.

## 다시 켜는 방법
`app/lib/config/feature_flags.dart`의 `kKoreanHistoryEnabled`를 `true`로 바꾸고 커밋·푸시하면 4곳 모두 자동으로 다시 노출됨(추가 코드 수정 불필요).

---

## 오후 세션 — 홈 화면 OTT(넷플릭스/디즈니+/쿠팡플레이) 스타일 개편 + PC 반응형 + 상단 메뉴 재편

### 1. PC/데스크톱 반응형 레이아웃
- `app/lib/theme/app_theme.dart`에 `kDesktopBreakpoint = 900` 추가.
- `main.dart`: 900 미만은 기존처럼 폰 폭(430)/태블릿 폭(640) 제한, 900 이상은 폭 제한 없이 `RootScreen`이 자체 레이아웃(최대 1200 중앙 정렬 콘텐츠 + 상단 가로 헤더)을 담당하도록 변경.
- 처음엔 왼쪽 사이드바 형태로 만들었다가 "일반 웹사이트처럼 보이지 않는다"는 피드백을 받아 **사이드바를 없애고 화면 폭 전체를 가로지르는 상단 헤더**(`_DesktopHeader`, 로고+메뉴)로 교체. 이후 우측 상단 CTA 버튼도 제거.

### 2. 홈 화면 다크 OTT 스타일 개편 (`app/lib/theme/ott_theme.dart` 신규 + `home_screen.dart` 전면 재작성)
- `OttColors`(근블랙 배경, 다크 카드, 블루→퍼플 브랜드 그라데이션 포인트) 팔레트 신설. 홈 탭이 선택된 동안만 상단 내비게이션도 다크로 전환되고, 자격증/마이페이지는 기존 라이트 테마 유지.
- 구성: 롤링배너 → 히어로 배너(CTA) → 과목별 학습(가로 스크롤) → 민법 특강(가로 스크롤) → 데일리 OX퀴즈 → 수강후기.
- **포스터 이미지 톤다운 버그**: 표지 이미지에 거의 검정에 가까운 색으로 `BlendMode.multiply`를 걸었더니 이미지 자체가 안 보이는 문제 발생 → 약한 검정 오버레이(alpha 0.18)+하단 그라데이션 스크림 방식으로 수정.
- **가로 스크롤 카드 크기**: 처음엔 과목별 학습에 옛 가로형(1402x1122, 1.25 비율) 표지를 썼다가 민법 특강 세로 포스터(138x196, 0.71 비율) 카드와 안 맞아 양옆이 잘리는 문제 발견 → 이후 사용자가 새 과목 표지 이미지(경제법.png/민법.png, 1056x1490 세로형)를 만들어 와서 포스터 카드 규격(`kLecturePosterWidth/Height`)으로 통일.
- **마우스 휠 가로 스크롤**: `widgets/hscroll_list.dart` 신규 — `PointerScrollEvent`를 가로 스크롤 오프셋으로 변환해 마우스 휠로 카드 행을 좌우로 넘길 수 있게 함(넷플릭스 웹 동작 방식).
- **각 그리드 "더보기"**: `_RowHeader` 위젯(제목+더보기)과 전용 화면 `lecture_grid_screen.dart`/`subject_grid_screen.dart` 신규(각각 `civilLawLectures`/`examSubjects` 데이터를 Wrap으로 그리드 표시). 민법 특강 데이터는 `data/lecture_data.dart`로 분리해 홈 행과 더보기 화면이 공유.
- **데일리 OX퀴즈**: 처음엔 회차별 카드 그리드였다가, "회차 나누지 말고 대표 카드 하나만"으로 변경 → 민법 특강 포스터와 동일 규격의 단일 카드(`_DailyOxMainCard`)로 축소, 표지 이미지(`daily_ox_cover.png`, 사용자 제공) 적용.
- **응원 마키바(`marquee_text.dart`) 버그**: 컨테이너 폭과 무관하게 항상 고정 4개 사본만 그려서 넓은 데스크톱 화면에서 글자 사이 빈 공백이 보이는 버그 발견 → `LayoutBuilder`로 실제 폭에 맞춰 필요한 만큼 사본을 채우도록 수정. 데스크톱에서는 응원바 높이/글자 크기도 확대(25→34px), 폭도 콘텐츠(최대 1200)에 맞춰 전체 화면 폭이 아닌 콘텐츠 폭으로 정렬.
- **인트로 영상 → 롤링배너로 전환**: 처음엔 영상 자리(고정 높이 160)로 만들고 STUDY BOX 타이틀을 오버레이했다가, "영상 대신 3단 롤링배너로, 타이틀 오버레이는 제거"로 최종 변경 → 기존 `widgets/rolling_banner.dart`(비율 2172:724 ≈ 3:1)를 재사용, 다크 테마용 인디케이터 색상 파라미터(`activeDotColor`/`inactiveDotColor`) 추가. 실제 배너 이미지는 미정이라 그라데이션 플레이스홀더 3장.
- 히어로 배너 태그라인/CTA 버튼 폰트 크기를 여러 차례 조정(처음 확대 요청 → 이후 학습하기 버튼만 살짝 축소).

### 3. 상단 메뉴 5개로 재편
`root_screen.dart`: 홈(0)/자격증(1)/공지사항(2)/FAQ(3)/마이페이지(4) 5탭 구조로 변경. 기존에 홈 화면 하단 게시판 형태로 있던 자주 묻는 질문/공지사항 섹션은 제거하고 `NoticeScreen`/`FaqScreen`을 독립 탭으로 승격.
- **부수 발견 버그**: `FaqScreen`의 `ExpansionTile`이 배경색 있는 `Container`(DecoratedBox) 안에 있어서 "ListTile background color or ink splashes may be invisible" 예외 발생 — 기존에는 지연 push라 노출 안 됐다가, 탭이 되며 `IndexedStack`이 5개 화면을 한꺼번에 미리 빌드하면서 처음 드러남. `Container` 배경색을 없애고 그 안을 `Material(color: ...)`로 감싸 수정.

### 롤링배너 제작 가이드(사용자 문의 답변)
`widgets/rolling_banner.dart`의 `AspectRatio`가 2172:724(≈3:1) — 이 비율에 정확히 맞춰 제작하면 화면 크기와 무관하게 잘림 없이 표시됨. 권장: 2172×724 원본 비율 그대로(또는 1200×400, 1500×500 등 3:1 비율), PNG/JPG, 500KB~1MB 이내.

### 새로 추가된 파일
`theme/ott_theme.dart`, `widgets/hscroll_list.dart`, `data/lecture_data.dart`, `screens/lecture_grid_screen.dart`, `screens/subject_grid_screen.dart`, `assets/images/daily_ox_cover.png`(사용자 제공), `assets/images/subject_cover_economic_law.png`/`subject_cover_civil_law.png`(교체, 사용자 제공).

### 남은 것 (오전~오후 세션 기준, 이후 저녁 세션에서 갱신됨 — 맨 아래 최신 섹션 참고)
- 경영학 과목 표지 이미지 없음(placeholder 상태, 추후 추가 예정 — 이미지 오면 `home_screen.dart`의 `subjectCoverImage()`에 매핑 추가).
- 롤링배너 실제 이미지 3장 없음(현재 그라데이션 placeholder).
- `flutter analyze`+`flutter test`(5개) 통과 확인 완료.

---

## 저녁 세션 — 히어로 텍스트 크기 조정, 인트로 영상 삽입, 모바일 내비게이션 재편

### 1. 히어로 배너 제목/부제 크기 (PC)
`_HeroBanner`(home_screen.dart) 제목·부제 폰트 크기를 여러 차례 시행착오로 조정:
- 부제를 제목만큼 키웠다가("제목만큼은 키워야") → 너무 커서("ㅋㅋㅋ 너무 큰데") 24px로 축소 → 결국 제목도 24px로 낮춰 **제목/부제 둘 다 24px(PC), 모바일은 기존 23px/14px 유지**로 정착.
- 부제는 `FittedBox(fit: BoxFit.scaleDown)`으로 감싸 폭이 좁아져도 항상 한 줄 유지(줄바꿈 안 됨).
- ⚠️ 참고: 배포 후 "그대로인 것 같다"는 피드백이 여러 번 있었는데, 실제로는 **Flutter 웹 서비스워커 캐시** 때문— 시크릿 창으로 확인하면 바로 보임(일반 새로고침/Ctrl+F5로도 안 지워질 때 있음).

### 2. 인트로 영상 삽입 (`widgets/intro_video.dart` 신규, `video_player: ^2.9.2` 의존성 추가)
- 사용자가 제공한 실제 영상 파일(`스터디박스 인트로영상.mp4`, 다운로드 폴더)을 `assets/videos/studybox_intro.mp4`로 프로젝트에 추가.
- 응원바 바로 아래, 기존 롤링배너 자리에 삽입. **음소거 자동재생 + `setLooping(true)`** 로 끊김 없는 무한 반복(웹 기준 자동재생은 반드시 음소거여야 브라우저가 허용).
- 위젯 테스트 환경(headless, 실제 비디오 플랫폼 구현 없음)에서 `initialize()`가 예외를 던져 테스트가 깨지는 문제 발견 → `.catchError()`로 조용히 무시하고 플레이스홀더(검은 배경)로 남도록 처리. **위젯 테스트에서 video_player를 쓸 때는 항상 이 예외 처리가 필요함.**
- **비율/크롭 여러 차례 조정**: 16:9 → 밝기/그라데이션 오버레이 추가(alpha 0.22 + 상하 그라데이션, 너무 어둡지 않게) → "높이가 너무 크다"는 피드백으로 21:9 컨테이너 + `BoxFit.contain`으로 전환(원본 안 잘리고 높이만 축소, 좌우에 레터박스).
- **"영상을 왼쪽 정렬해달라"** → `LayoutBuilder`로 영상의 실제 비율(`_controller.value.aspectRatio`)을 기준으로 렌더링 폭을 계산해 `Align(centerLeft)`로 왼쪽 배치, 오른쪽에 생기는 여백에 **세로 롤링배너**(`widgets/vertical_rolling_banner.dart` 신규, `PageView(scrollDirection: Axis.vertical)` + `Timer.periodic`으로 4초마다 자동 전환)를 배치.
- 우측 상단에 "STUDY BOX" 타이틀 오버레이 추가(GoogleFonts.blackHanSans).
- 응원바 문구를 "네모난 화면 속, 나만의 합격 상자가 열린다"로 교체.

### 3. 모바일 전용 레이아웃 분리 (같은 세션 후반)
"모바일에서 영상 옆 세로배너가 잘 안 보인다"는 피드백으로 `IntroVideo`를 반응형으로 분리:
- **데스크톱**(`_buildDesktop`): 위 2번의 왼쪽 정렬 영상 + 오른쪽 세로 롤링배너 유지.
- **모바일**(`_buildMobile`): 영상을 전체 폭 16:9로 보여주고, **그 아래**에 기존 `widgets/rolling_banner.dart`(가로 스크롤, 비율 2172:724≈3:1)를 재사용해 가로로 넘어가는 배너를 배치. STUDY BOX 오버레이 폰트도 모바일에서 18→14px로 축소.
- 공통 렌더링 로직(`_videoSurface`)을 메서드로 추출해 데스크톱/모바일이 공유.

### 4. 모바일 내비게이션 재편
- **하단 "지금 학습 시작하기" 바 완전 삭제** — `_BottomStudyBar` 위젯, `_HomeDepthObserver`(스크롤 깊이 추적용 옵저버), `_homeAtRoot` 상태, `_goToLicenseTab` 메서드까지 전부 제거(더 이상 쓰는 곳 없음).
- **상단 탭바 재편**: 모바일 `_TopNavBar`가 이제 홈/자격증/공지사항/FAQ **4개만** 아이콘 탭으로 보여주고, 오른쪽 끝에 **삼선(☰) 아이콘**을 추가. 누르면 `showModalBottomSheet`로 회원가입/로그인/마이페이지 3개 항목이 뜨는 바텀시트가 열림(마이페이지 클릭 시 탭 인덱스 4로 이동). PC 데스크톱 헤더(회원가입·로그인·마이페이지가 우측에 있는 구성)와 맥락을 맞춘 것.
- 히어로 CTA("지금 학습 시작하기") 버튼 크기 소폭 축소(패딩 26/15→22/12, 아이콘 22→19, 글자 20→17).
- 위젯 테스트(`quiz_completion_test.dart`)가 하단바 Key로 탭하던 부분을 상단 탭바의 "자격증" 텍스트를 탭하도록 수정.

### 롤링배너 사이즈 가이드 (2차 — 세로 배너 포함)
- **모바일 가로 배너**(영상 아래): 기존과 동일 2172:724(≈3:1). 추천 2172×724 또는 1200×400.
- **PC 세로 배너**(영상 오른쪽 여백): 영상(16:9, 21:9 박스 안 왼쪽 정렬) 옆에 남는 비율이라 대략 **9:16(세로, 인스타 스토리 비율)** 로 제작하면 화면 크기가 달라져도 비율이 유지됨. 추천 360×640 또는 540×960.
- ⚠️ **PC 세로 배너는 아직 이미지 지원 코드가 없음** — 지금은 색상+텍스트 placeholder(`vertical_rolling_banner.dart`의 `_slides` 상수). 실제 이미지 받으면 `BannerItem`처럼 `imageAsset` 필드를 추가하는 작업이 필요함.

### 최종 남은 것 (다음 세션 시작점)
- [ ] PC 세로 롤링배너 실제 이미지 3장 없음 + 이미지 지원 코드 미구현(현재 색상 placeholder).
- [ ] 모바일 가로 롤링배너(영상 아래) 실제 이미지 3장 없음(현재 그라데이션 placeholder, `intro_video.dart`의 `_buildMobile` 안에 하드코딩).
- [ ] 경영학 과목 표지 이미지 없음(placeholder).
- [ ] 인트로 영상은 현재 1개 고정(`assets/videos/studybox_intro.mp4`) — 교체하려면 같은 파일명으로 덮어쓰기만 하면 됨.
- [ ] `flutter analyze`+`flutter test`(5개) 통과 확인 완료, 매 커밋마다 검증 후 배포함.
- [ ] 회원가입/로그인/마이페이지는 전부 준비중 스낵바만 뜨는 상태(실제 인증 기능 없음) — CLAUDE.md의 "남은 작업"에 이미 기재된 항목.
