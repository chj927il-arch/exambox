# 작업 로그 — 2026-07-30

가맹거래사 1차 시험대비 앱 "스터디박스" (gamyeong-exam) — 앱 방향 재확정: **가맹거래사 1차 시험 대비 앱으로 개편**, 한국사능력검정시험(한능검) 관련 콘텐츠는 삭제가 아닌 **비노출(준비 중) 처리**.

## 배경

7/26~29 사이 STUDY BOX 특강 메뉴를 신설하면서 한능검(한국사능력검정시험) 콘텐츠(자격증 메뉴, 홈 화면 배너/특강 그리드, 데일리 OX 필터)가 함께 추가됨. 앱의 본래 목적을 가맹거래사 1차 시험 대비로 명확히 하기로 하고, 한능검 콘텐츠는 **완전히 지우지 않고** 나중에 다시 켤 수 있도록 스위치 하나로 숨김.

## 변경 내용

### `app/lib/config/feature_flags.dart` (신규)
- `const bool kKoreanHistoryEnabled = false;` 플래그 하나로 한능검 노출 여부 전체 제어.
- **다시 노출하려면 이 값을 `true`로 바꾸고 재빌드/재배포하면 됨.** 데이터·화면 코드·이미지 에셋은 전혀 건드리지 않음(전부 그대로 남아있음).

### 숨긴 지점 4곳 (모두 `kKoreanHistoryEnabled`로 조건부 렌더링)
1. `app/lib/screens/license_screen.dart` — 자격증 탭 '취업/기타' 카테고리의 '한국사능력검정' 항목을 `ready: false`로 전환(다른 준비중 자격증과 동일하게 "준비 중이에요" 토스트만 뜸).
2. `app/lib/screens/home_screen.dart`
   - 롤링배너(`STUDY BOX 콘텐츠`)의 한국사 배너 카드 숨김.
   - `_SubjectGuideCarousel`(과목 안내)의 한국사능력검정시험 표지 카드 숨김.
   - '한능검 특강' 섹션 타이틀 + `_KoreanHistoryLectureGrid`(고려/조선/근현대사 카드) 전체 숨김.
3. `app/lib/screens/daily_ox_list_screen.dart` — 데일리 OX 퀴즈 화면의 자격증 필터 칩에서 '한국사능력검정' 제거(가맹거래사만 노출).

### 건드리지 않은 것 (삭제 아님, 그대로 보존)
- `korean_history_intro_screen.dart`, `korean_history_level_screen.dart`, `lecture_intro_screen.dart` 등 화면 파일
- `topic_stats.dart`/`sample_questions.dart`/`ox_quiz_data.dart`의 한국사 데이터
- `subject_style.dart`의 `korean_history`/`korean_history_basic` 스타일 정의
- 표지·배너·특강 이미지 에셋 전부

→ 코드/데이터는 그대로 있고 진입 경로만 막혀 있는 상태. `flutter analyze` 통과 확인.

## 다시 켜는 방법
`app/lib/config/feature_flags.dart`의 `kKoreanHistoryEnabled`를 `true`로 바꾸고 커밋·푸시하면 4곳 모두 자동으로 다시 노출됨(추가 코드 수정 불필요).
